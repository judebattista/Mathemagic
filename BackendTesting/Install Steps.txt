
{ NOTES FROM: https://www.youtube.com/watch?v=uZgRbnIsgrA }
	ssh Student@52.37.100.134
	1qa2ws#ED44
	. BackendTesting/venv/bin/activate
	sudo python BackendTesting/Mathemagic/BackendTesting/backend/src manage.py runserver 172.31.40.19:8000
{
REALLY QUICK START:
	
}

{	
INSTALL STEPS:
	sudo -i
	sudo apt install python 3.7-venv 	# Install the venv
	sudo python -m venv venv		# Make the venv
	. venv/bin/activate			# Source the venv
	sudo pip install django			# Install django
	sudo pip install djangorestframework	# Install the rest framework
	cd ~
	sudo mkdir ~/.nvm
	sudo git clone https://github.com/nvm-sh/nvm.git "~/.nvm"
	cd .nvm
	git checkout `git describe --abbrev=0 --tags --match "v[0-9]*" $(git rev-list --tags --max-count=1)`
	. ~/.nvm/nvm.sh
	{Add some things to .bashrc and .profile in ~}
	sudo git clone https://github.com/judebattista/Mathemagic.git
	cd Mathemagic
	sudo git checkout BackendTesting
	sudo python manage.py runserver 52.37.100.134:8000
	{docker-compose up}
}

{
    QUICK START GUIDE (For personal use on PC):
    In one git bash terminal, run this:
    {
        cd backend
        source venv/Scripts/activate
        {
            python src/manage.py makemigrations
            python src/manage.py migrate
        }
        python src/manage.py runserver
    }

    In another, run this:
    {
        cd backend/gui
        npm start
    }
}


python -m pip install --upgrade pip
python -m venv ./venv
source venv/Scripts/activate
pip install django
pip install djangorestframework
django-admin startproject SWENGDjangoBackend

{Install three extensions for VS code from here: https://automationpanda.com/2018/02/08/django-projects-in-visual-studio-code/}

pip install pep8
pip install autopep8
pip install pylint

{modify settings.json to have correct settings - did this even work?}

{install node: https://github.com/coreybutler/nvm-windows/releases/download/1.1.7/nvm-setup.zip}

{restart visual studio after this...}
{also might need to start visual studio as administrator}

nvm install 12.16.1
nvm use 12.16.1

{in ./frontend}

npm install -g create-react-app
npx create-react-app gui

python manage.py migrate

python manage.py runserver

python manage.py startapp articles

python manage.py makemigrations {after you make a change to a model}
python manage.py migrate {?}

python manage.py createsuperuser

{
    admin
    1234
    rrutherford20@my.whitworth.edu
}

{cd into frontend/gui}

npm start

{C:/Python/python -m pip install -U pycodestyle --user}

npm install antd --save
npm install axios --save
npm install react-router-dom --save

pip install django-cors-headers

npm i --save react-redux
npm i --save redux redux-thunk

pip install django-rest-auth

python manage.py migrate

pip install django-allauth